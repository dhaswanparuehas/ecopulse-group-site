<section class="hero" aria-label="Ecopulse Hero">
  <div class="hero-inner">
    <h1>
      Cooling Resilience Infrastructure
      <span>for a Volatile Energy World</span>
    </h1>

    <p class="subtitle">
      Ecopulse builds thermal energy infrastructure that protects mission-critical buildings
      from peak demand, grid stress, and energy volatility — powered by PCM-based energy storage.
    </p>

    <div class="cta">
      <a class="btn primary" href="/contact">Talk to Ecopulse</a>
      <a class="btn ghost" href="/investors">Request Investor Deck</a>
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    padding: 6rem 1.5rem;
    position: relative;

    /* background image */
    background-image:
      linear-gradient(180deg, rgba(0,0,0,.55) 0%, rgba(0,0,0,.35) 45%, rgba(0,0,0,.55) 100%),
      url("/images/ChatGPT Image 4 ก.พ. 2569 22_24_53.png");
    background-size: cover;
    background-position: center;
  }

  .hero-inner {
    width: 100%;
    max-width: 980px;
    margin: 0 auto;
    color: #fff;
    text-align: center;
  }

  h1 {
    margin: 0;
    font-size: clamp(2rem, 4.2vw, 3.4rem);
    line-height: 1.08;
    font-weight: 750;
    letter-spacing: -0.02em;
  }

  h1 span {
    display: block;
    opacity: 0.95;
    margin-top: 0.25rem;
  }

  .subtitle {
    max-width: 820px;
    margin: 1.25rem auto 0;
    font-size: clamp(1rem, 1.5vw, 1.25rem);
    line-height: 1.6;
    opacity: 0.92;
  }

  .cta {
    margin-top: 1.75rem;
    display: flex;
    gap: 0.75rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.85rem 1.1rem;
    border-radius: 999px;
    text-decoration: none;
    font-weight: 650;
    font-size: 1rem;
    border: 1px solid rgba(255,255,255,0.25);
    backdrop-filter: blur(6px);
  }

  .btn.primary {
    background: rgba(255,255,255,0.92);
    color: #0b1220;
    border-color: rgba(255,255,255,0.0);
  }

  .btn.ghost {
    background: rgba(0,0,0,0.25);
    color: #fff;
  }
</style>
